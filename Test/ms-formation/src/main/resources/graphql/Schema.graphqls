type Formation {
    id: ID
    intitule: String
    niveau: String
    modules: [Module]
}

type Module {
    id: ID
    titre: String
    enseignant: Enseignant
}

type Enseignant {
    nom: String
}

type Query {
    formationsByFaculte(facId: ID): [Formation]
}